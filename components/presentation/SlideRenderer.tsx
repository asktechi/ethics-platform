/**
 * Retired. Audience-facing slide content renders only through AudienceMirror.
 * Host jump-grid / next-up chrome uses SlideThumb — never fork a second slide stage.
 */

export { AudienceMirror as SlideRenderer } from "@/components/presentation/AudienceMirror";
